<UserControl x:Class="POS.UI.Modules.Utilities.SystemHealth.SystemHealthView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Background="{StaticResource BrushWindowBg}" Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <DockPanel Grid.Row="0" Margin="0,0,0,12">
            <TextBlock Text="System Diagnostics" FontSize="28" FontWeight="Bold"
                       Foreground="{StaticResource BrushTextPrimary}" DockPanel.Dock="Left"/>
            <Button Content="Run Diagnostics"
                    Command="{Binding CheckAllCommand}"
                    HorizontalAlignment="Right"
                    Padding="16,10"
                    Style="{StaticResource PrimaryButtonStyle}"/>
        </DockPanel>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <ItemsControl ItemsSource="{Binding Services}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel IsItemsHost="True" Orientation="Horizontal"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Width="300" Padding="15" Margin="8"
                                Background="{StaticResource BrushCardBg}"
                                CornerRadius="8"
                                BorderBrush="{StaticResource BrushBorderSoft}"
                                BorderThickness="1">
                            <Border.Effect>
                                <DropShadowEffect BlurRadius="10" ShadowDepth="2" Opacity="0.15"/>
                            </Border.Effect>
                            <StackPanel>
                                <TextBlock Text="{Binding Name}" FontSize="18" FontWeight="Bold" />
                                <TextBlock Text="{Binding Url}" Foreground="{StaticResource BrushTextSecondary}" FontSize="12" Margin="0,2,0,8"/>
                                <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="0,4,0,0">
                                    <Ellipse Width="12" Height="12" Fill="{Binding StatusColor}" Margin="0,0,8,0"/>
                                    <TextBlock Text="{Binding IsOnline, Converter={StaticResource BoolToTextConverter}, ConverterParameter='Online|Offline'}"
                                               FontWeight="SemiBold"/>
                                </StackPanel>
                                <TextBlock Text="{Binding LatencyString}" Foreground="{StaticResource BrushTextSecondary}" Margin="0,4,0,0"/>
                                <ProgressBar IsIndeterminate="True" Height="3" Margin="0,8,0,0"
                                             Visibility="{Binding IsChecking, Converter={StaticResource BoolToVisibilityConverter}}"/>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</UserControl>
