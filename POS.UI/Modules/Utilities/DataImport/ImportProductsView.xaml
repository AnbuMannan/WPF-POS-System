<UserControl x:Class="POS.UI.Modules.Utilities.DataImport.ImportProductsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,12">
            <TextBlock Text="Data Import / Export" FontSize="20" FontWeight="Bold" Margin="0,0,0,4"/>
            <TextBlock Text="Bulk upload products using the Excel template." FontSize="12" Foreground="Gray"/>
        </StackPanel>

        <Border Grid.Row="1" Background="White" Padding="16" CornerRadius="8" BorderBrush="#E0E0E0" BorderThickness="1">
            <StackPanel>
                <TextBlock Text="Steps" FontWeight="SemiBold" Margin="0,0,0,8"/>
                <TextBlock Text="1. Download the template" FontSize="12" Margin="0,0,0,2"/>
                <TextBlock Text="2. Fill in product data" FontSize="12" Margin="0,0,0,2"/>
                <TextBlock Text="3. Upload the completed file" FontSize="12" Margin="0,0,0,12"/>

                <StackPanel Orientation="Horizontal" Margin="0,0,0,16">
                    <Button Content="Download Template"
                            Width="160"
                            Height="32"
                            Margin="0,0,12,0"
                            Command="{Binding DownloadTemplateCommand}"/>
                </StackPanel>

                <Border Background="#F5F5F5" Padding="12" CornerRadius="6">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0">
                            <TextBlock Text="Select Excel file (.xlsx)" FontWeight="SemiBold" Margin="0,0,0,4"/>
                            <TextBox Text="{Binding FilePath}" IsReadOnly="True" Height="32" Margin="0,0,0,4"/>
                            <Button Content="Browse..." Width="100" Height="30" Command="{Binding BrowseCommand}"/>
                        </StackPanel>

                        <Button Grid.Column="1"
                                Content="Upload"
                                Width="140"
                                Height="40"
                                Margin="16,0,0,0"
                                VerticalAlignment="Center"
                                FontSize="14"
                                Command="{Binding UploadCommand}"/>
                    </Grid>
                </Border>
            </StackPanel>
        </Border>

        <Border Grid.Row="2"
                Margin="0,12,0,0"
                Padding="12"
                CornerRadius="6"
                Background="#F0F0F0"
                BorderBrush="#E0E0E0"
                BorderThickness="1">
            <TextBlock Text="{Binding ImportSummary}"
                       TextWrapping="Wrap"
                       FontSize="12"
                       Foreground="Black"/>
        </Border>
    </Grid>
</UserControl>

