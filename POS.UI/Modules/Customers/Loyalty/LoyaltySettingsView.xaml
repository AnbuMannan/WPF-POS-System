<UserControl x:Class="POS.UI.Modules.Customers.Loyalty.LoyaltySettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="Loyalty Settings"
                   FontSize="20"
                   FontWeight="Bold"
                   Foreground="{StaticResource BrushTextPrimary}"
                   Margin="0,0,0,16"/>

        <Border Grid.Row="1"
                Background="{StaticResource BrushCardBg}"
                Padding="20"
                CornerRadius="8"
                BorderBrush="{StaticResource BrushBorderSoft}"
                BorderThickness="1">
            <StackPanel>
                <TextBlock Text="Earn points configuration"
                           FontWeight="SemiBold"
                           FontSize="14"
                           Margin="0,0,0,8"/>
                <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                    <TextBlock Text="Earn"
                               VerticalAlignment="Center"
                               Margin="0,0,4,0"/>
                    <TextBox Width="80"
                             Text="{Binding PointsPerUnitCurrency, UpdateSourceTrigger=PropertyChanged}"
                             Style="{StaticResource FormTextBox}"
                             VerticalAlignment="Center"
                             Margin="0,0,4,0"/>
                    <TextBlock Text="points for every ₹1 spent"
                               VerticalAlignment="Center"/>
                </StackPanel>

                <TextBlock Text="Redemption configuration"
                           FontWeight="SemiBold"
                           FontSize="14"
                           Margin="0,16,0,8"/>
                <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                    <TextBlock Text="Each point is worth ₹"
                               VerticalAlignment="Center"
                               Margin="0,0,4,0"/>
                    <TextBox Width="80"
                             Text="{Binding RedemptionValuePerPoint, UpdateSourceTrigger=PropertyChanged}"
                             Style="{StaticResource FormTextBox}"
                             VerticalAlignment="Center"/>
                </StackPanel>

                <StackPanel Orientation="Horizontal" Margin="0,0,0,20">
                    <TextBlock Text="Minimum points to redeem:"
                               VerticalAlignment="Center"
                               Margin="0,0,4,0"/>
                    <TextBox Width="80"
                             Text="{Binding MinimumRedeemPoints, UpdateSourceTrigger=PropertyChanged}"
                             Style="{StaticResource FormTextBox}"
                             VerticalAlignment="Center"/>
                </StackPanel>

                <StackPanel Orientation="Horizontal"
                            HorizontalAlignment="Right">
                    <Button Content="Save"
                            Width="120"
                            Style="{StaticResource PrimaryButton}"
                            Command="{Binding SaveCommand}"/>
                </StackPanel>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
