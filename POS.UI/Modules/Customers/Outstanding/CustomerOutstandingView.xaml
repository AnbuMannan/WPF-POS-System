<UserControl x:Class="POS.UI.Modules.Customers.Outstanding.CustomerOutstandingView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             KeyDown="UserControl_KeyDown"
             Focusable="True">

    <UserControl.Resources>
        <!-- Balance Display Styles -->
        <Style x:Key="BalanceTextStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="24"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="#D32F2F"/>
        </Style>

        <Style x:Key="LabelStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Foreground" Value="#666666"/>
            <Setter Property="Margin" Value="0,0,0,4"/>
        </Style>

        <Style x:Key="FormLabelStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="#333333"/>
            <Setter Property="Margin" Value="0,0,0,6"/>
        </Style>

        <Style x:Key="SectionHeaderStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="#1976D2"/>
            <Setter Property="Margin" Value="0,0,0,10"/>
        </Style>

        <Style x:Key="CardStyle" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="BorderBrush" Value="#E0E0E0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="16"/>
        </Style>
    </UserControl.Resources>

    <Grid Background="#F5F5F5">
        <Grid.ColumnDefinitions>
            <!-- Left Panel: Customer List with Balances -->
            <ColumnDefinition Width="400"/>
            <!-- Right Panel: Details + Payment -->
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- ==================== LEFT PANEL: CUSTOMER LIST ==================== -->
        <Border Grid.Column="0" Style="{StaticResource CardStyle}" Margin="10">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Header -->
                <StackPanel Grid.Row="0" Margin="0,0,0,12">
                    <TextBlock Text="Customers" Style="{StaticResource SectionHeaderStyle}"/>
                    <TextBlock Text="Select a customer to view ledger and receive payment" 
                               FontSize="12" Foreground="#888888"/>
                </StackPanel>

                <!-- Search Box -->
                <Grid Grid.Row="1" Margin="0,0,0,12">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <TextBox x:Name="SearchBox"
                             Grid.Column="0"
                             Height="36"
                             Padding="10,8"
                             Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                             Tag="Search customers..."/>

                    <Button Grid.Column="1"
                            Content="↻"
                            Width="36"
                            Height="36"
                            Margin="8,0,0,0"
                            ToolTip="Refresh (F5)"
                            Style="{StaticResource SecondaryButtonStyle}"
                            Command="{Binding RefreshCommand}"/>
                </Grid>

                <!-- Customer List -->
                <ListBox Grid.Row="2"
                         ItemsSource="{Binding FilteredCustomers}"
                         SelectedItem="{Binding SelectedCustomer}"
                         BorderThickness="0"
                         Background="Transparent"
                         ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Border Padding="12" Margin="0,0,0,6" 
                                    Background="#FAFAFA" CornerRadius="6"
                                    BorderBrush="#E8E8E8" BorderThickness="1">
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>

                                    <!-- Customer Name -->
                                    <TextBlock Grid.Row="0" Grid.Column="0"
                                               Text="{Binding CustomerName}"
                                               FontWeight="SemiBold"
                                               FontSize="14"/>

                                    <!-- Balance -->
                                    <TextBlock Grid.Row="0" Grid.Column="1"
                                               Text="{Binding CurrentBalance, StringFormat='₹ {0:N2}'}"
                                               FontWeight="Bold"
                                               FontSize="14"
                                               Foreground="#D32F2F"/>

                                    <!-- Phone -->
                                    <TextBlock Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2"
                                               Margin="0,4,0,0"
                                               FontSize="11" Foreground="#888888">
                                        <Run Text="{Binding Phone, Mode=OneWay}"/>
                                    </TextBlock>

                                    <!-- Last Transaction -->
                                    <TextBlock Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="2"
                                               Margin="0,4,0,0"
                                               FontSize="10" Foreground="#AAAAAA">
                                        <Run Text="Last: "/>
                                        <Run Text="{Binding LastTransactionDate, StringFormat='dd-MMM-yyyy', TargetNullValue='No transactions'}"/>
                                    </TextBlock>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                    <ListBox.ItemContainerStyle>
                        <Style TargetType="ListBoxItem">
                            <Setter Property="Padding" Value="0"/>
                            <Setter Property="Margin" Value="0"/>
                            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="ListBoxItem">
                                        <ContentPresenter/>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </ListBox.ItemContainerStyle>
                </ListBox>
            </Grid>
        </Border>

        <!-- ==================== RIGHT PANEL: BALANCE + PAYMENT + LEDGER ==================== -->
        <Grid Grid.Column="1" Margin="0,10,10,10">
            <Grid.RowDefinitions>
                <!-- Balance Display -->
                <RowDefinition Height="Auto"/>
                <!-- Payment Form -->
                <RowDefinition Height="Auto"/>
                <!-- Transaction Ledger -->
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Selected Customer Balance Display -->
            <Border Grid.Row="0" Style="{StaticResource CardStyle}" Margin="0,0,0,10">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0" VerticalAlignment="Center">
                        <TextBlock Text="Selected Customer" Style="{StaticResource LabelStyle}"/>
                        <TextBlock Text="{Binding SelectedCustomer.CustomerName, FallbackValue='No customer selected'}"
                                   FontSize="18" FontWeight="SemiBold"/>
                        <TextBlock Text="{Binding SelectedCustomer.Phone, FallbackValue=''}"
                                   FontSize="12" Foreground="#888888"/>
                    </StackPanel>

                    <StackPanel Grid.Column="1" HorizontalAlignment="Right" VerticalAlignment="Center">
                        <TextBlock Text="Outstanding Balance" Style="{StaticResource LabelStyle}" 
                                   HorizontalAlignment="Right"/>
                        <TextBlock Text="{Binding SelectedCustomer.CurrentBalance, StringFormat='₹ {0:N2}', FallbackValue='₹ 0.00'}"
                                   Style="{StaticResource BalanceTextStyle}"
                                   HorizontalAlignment="Right"/>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- Payment Form -->
            <Border Grid.Row="1" Style="{StaticResource CardStyle}" Margin="0,0,0,10"
                    IsEnabled="{Binding IsCustomerSelected}">
                <StackPanel>
                    <TextBlock Text="Receive Payment" Style="{StaticResource SectionHeaderStyle}"/>

                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="20"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="20"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <!-- Amount -->
                        <StackPanel Grid.Row="0" Grid.Column="0">
                            <TextBlock Text="Amount (₹)" Style="{StaticResource FormLabelStyle}"/>
                            <TextBox x:Name="AmountTextBox"
                                     Text="{Binding PaymentAmount, UpdateSourceTrigger=PropertyChanged, StringFormat=N2}"
                                     Height="36"
                                     FontSize="16"
                                     FontWeight="Bold"
                                     HorizontalContentAlignment="Right"/>
                        </StackPanel>

                        <!-- Payment Mode -->
                        <StackPanel Grid.Row="0" Grid.Column="2">
                            <TextBlock Text="Payment Mode" Style="{StaticResource FormLabelStyle}"/>
                            <ComboBox ItemsSource="{Binding PaymentModes}" 
                                      SelectedItem="{Binding PaymentMode}"
                                      Height="36"/>
                        </StackPanel>

                        <!-- Remarks -->
                        <StackPanel Grid.Row="0" Grid.Column="4">
                            <TextBlock Text="Remarks" Style="{StaticResource FormLabelStyle}"/>
                            <TextBox Text="{Binding PaymentRemarks, UpdateSourceTrigger=PropertyChanged}"
                                     Height="36"/>
                        </StackPanel>

                        <!-- Action Buttons -->
                        <StackPanel Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="5"
                                    Orientation="Horizontal" 
                                    HorizontalAlignment="Right"
                                    Margin="0,16,0,0">

                            <Button Content="Clear (Esc)"
                                    Width="100"
                                    Height="36"
                                    Margin="0,0,10,0"
                                    Style="{StaticResource SecondaryButtonStyle}"
                                    Command="{Binding ClearFormCommand}"/>

                            <Button Content="Receive Payment (F2)"
                                    Width="170"
                                    Height="36"
                                    Style="{StaticResource SuccessButtonStyle}"
                                    Command="{Binding ReceivePaymentCommand}"/>
                        </StackPanel>
                    </Grid>
                </StackPanel>
            </Border>

            <!-- Transaction Ledger -->
            <Border Grid.Row="2" Style="{StaticResource CardStyle}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" Text="Transaction Ledger" Style="{StaticResource SectionHeaderStyle}"/>

                    <DataGrid Grid.Row="1"
                              ItemsSource="{Binding Transactions}"
                              AutoGenerateColumns="False"
                              IsReadOnly="True"
                              SelectionMode="Single"
                              BorderThickness="0"
                              RowBackground="White"
                              AlternatingRowBackground="#FAFAFA"
                              RowHeight="36"
                              FontSize="12"
                              AlternationCount="2"
                              GridLinesVisibility="Horizontal"
                              HeadersVisibility="Column">

                        <DataGrid.ColumnHeaderStyle>
                            <Style TargetType="DataGridColumnHeader">
                                <Setter Property="Background" Value="#263238"/>
                                <Setter Property="Foreground" Value="White"/>
                                <Setter Property="FontWeight" Value="Bold"/>
                                <Setter Property="FontSize" Value="12"/>
                                <Setter Property="Padding" Value="10,8"/>
                                <Setter Property="BorderBrush" Value="#1E272E"/>
                                <Setter Property="BorderThickness" Value="0,0,1,0"/>
                                <Setter Property="HorizontalContentAlignment" Value="Left"/>
                                <Setter Property="VerticalContentAlignment" Value="Center"/>
                                <Setter Property="MinHeight" Value="38"/>
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="DataGridColumnHeader">
                                            <Border Background="{TemplateBinding Background}"
                                                    BorderBrush="{TemplateBinding BorderBrush}"
                                                    BorderThickness="{TemplateBinding BorderThickness}"
                                                    Padding="{TemplateBinding Padding}">
                                                <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                                                  VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                                                  TextBlock.Foreground="White"
                                                                  TextBlock.FontWeight="Bold"
                                                                  TextBlock.FontSize="12"/>
                                            </Border>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </DataGrid.ColumnHeaderStyle>

                        <DataGrid.RowStyle>
                            <Style TargetType="DataGridRow">
                                <Setter Property="Cursor" Value="Hand"/>
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#E3F2FD"/>
                                    </Trigger>
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter Property="Background" Value="#BBDEFB"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </DataGrid.RowStyle>

                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Date" Width="100"
                                                Binding="{Binding TransactionDate, StringFormat='dd-MMM-yyyy'}">
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="VerticalAlignment" Value="Center"/>
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>

                            <DataGridTextColumn Header="Type" Width="100"
                                                Binding="{Binding TransactionType}">
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="VerticalAlignment" Value="Center"/>
                                        <Setter Property="FontWeight" Value="SemiBold"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding TransactionType}" Value="Payment">
                                                <Setter Property="Foreground" Value="#4CAF50"/>
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding TransactionType}" Value="Sale">
                                                <Setter Property="Foreground" Value="#D32F2F"/>
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding TransactionType}" Value="CreditNote">
                                                <Setter Property="Foreground" Value="#FF9800"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>

                            <DataGridTextColumn Header="Reference" Width="120"
                                                Binding="{Binding ReferenceNo}">
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="VerticalAlignment" Value="Center"/>
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>

                            <DataGridTextColumn Header="Description" Width="*"
                                                Binding="{Binding Description}">
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="VerticalAlignment" Value="Center"/>
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>

                            <DataGridTextColumn Header="Debit" Width="100">
                                <DataGridTextColumn.Binding>
                                    <Binding Path="DebitAmount" StringFormat="₹ {0:N2}"/>
                                </DataGridTextColumn.Binding>
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="HorizontalAlignment" Value="Right"/>
                                        <Setter Property="VerticalAlignment" Value="Center"/>
                                        <Setter Property="Foreground" Value="#D32F2F"/>
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>

                            <DataGridTextColumn Header="Credit" Width="100">
                                <DataGridTextColumn.Binding>
                                    <Binding Path="CreditAmount" StringFormat="₹ {0:N2}"/>
                                </DataGridTextColumn.Binding>
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="HorizontalAlignment" Value="Right"/>
                                        <Setter Property="VerticalAlignment" Value="Center"/>
                                        <Setter Property="Foreground" Value="#4CAF50"/>
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>

                            <DataGridTextColumn Header="Balance" Width="100">
                                <DataGridTextColumn.Binding>
                                    <Binding Path="Balance" StringFormat="₹ {0:N2}"/>
                                </DataGridTextColumn.Binding>
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="HorizontalAlignment" Value="Right"/>
                                        <Setter Property="VerticalAlignment" Value="Center"/>
                                        <Setter Property="FontWeight" Value="Bold"/>
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </Border>
        </Grid>

        <!-- Loading Overlay -->
        <Border Grid.Column="0" Grid.ColumnSpan="2"
                Background="#80000000"
                Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisibility}}">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                <TextBlock Text="Loading..." FontSize="18" Foreground="White"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
