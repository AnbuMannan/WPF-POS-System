<UserControl x:Class="POS.UI.Modules.Reports.Sales.ReportFilterControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,0,8">
            <TextBlock Text="From" VerticalAlignment="Center" Margin="0,0,8,0"/>
            <DatePicker SelectedDate="{Binding FromDate, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                        Width="140" Margin="0,0,16,0"/>
            <TextBlock Text="To" VerticalAlignment="Center" Margin="0,0,8,0"/>
            <DatePicker SelectedDate="{Binding ToDate, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                        Width="140" Margin="0,0,16,0"/>
            <ContentPresenter Content="{Binding ExtraFilters}" Margin="0,0,16,0"/>
            <Button Content="Generate"
                    Width="120"
                    Height="36"
                    Style="{StaticResource PrimaryButton}"
                    Command="{Binding GenerateCommand}"/>
        </StackPanel>
    </Grid>
</UserControl>
